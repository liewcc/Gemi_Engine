{
  "version": "1.1.1",
  "date": "2026-06-21",
  "notes": "Fix: sync is_running when browser is closed externally. Register context.on('close') in start() so the TUI button reflects real browser state after user closes the headed fallback window."
}
